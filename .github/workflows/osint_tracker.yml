name: OSINT Data Collector

on:
  schedule:
    - cron: '0 */6 * * *' # S'exécute automatiquement toutes les 6 heures
  workflow_dispatch: # Permet de le lancer manuellement en 1 clic
  push:
    paths:
      - 'collector.py'
      - 'communes.json'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code du dépôt
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Lancer le script collector
        run: python collector.py

      - name: Sauvegarder et publier (Méthode infaillible)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Récupère l'état exact du dépôt distant
          git fetch origin
          
          # Réinitialise l'historique local pour correspondre au distant, 
          # tout en gardant les modifications du fichier data_feed.json générées par le script
          git reset --soft origin/main
          
          git add data_feed.json
          
          # Vérifie s'il y a vraiment des changements à pousser
          if git diff-index --quiet HEAD --; then
            echo "Aucune nouvelle donnée à pousser."
          else
            git commit -m "Auto-update OSINT feed data [skip ci]"
            git push origin HEAD:main
            echo "Données mises à jour et poussées avec succès."
          fi
